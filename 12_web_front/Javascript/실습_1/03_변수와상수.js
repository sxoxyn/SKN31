/*
실행 : node파일명.js
*/

/*
  =============================================
  3. 변수와 상수
  =============================================
  변수는 값을 저장하거나 참조하기 위해 붙인 이름, 즉 값의 식별자이다.
  JavaScript에서는 변수를 선언할 때 const와 let을 사용한다.
*/

/*
  ---------------------------------------------
  3.1 const
  ---------------------------------------------
  const는 다른 값으로 다시 대입하지 않을 변수를 선언할 때 사용한다.
*/

// const name = "홍길동";
// const birthYear = 2000;
// console.log(name, birthYear);


/* 다른 값을 다시 대입하면 오류가 발생한다. */
{
  // const age = 20;
  // age = 21; // TypeError
}

/* 객체나 배열을 const로 선언하더라도 내부 값은 변경할 수 있다. */
{
  // const user = {
  //   name: "홍길동",
  //   age: 20,
  // }; // JavaScript 객체 정의

  // user.age = 21; // 가능
  // user = {};  // 불가능
  // console.log(user)
}

/*
  const는 객체 자체를 완전히 변경할 수 없게 만드는 문법이 아니다.
  변수에 새로운 객체를 다시 대입하지 못하게 한다.
*/

/*
  ---------------------------------------------
  3.2 let
  ---------------------------------------------
  let은 값이 바뀌는 변수를 선언할 때 사용한다.
*/
{
//   let score = 80;
//   console.log("let score값:", score);
//   score = 90;
//   console.log("변경 후 score값:", score);
}

/* 반복 횟수를 세거나 상태가 변하는 값을 저장할 때 사용한다. */
{
  // let count = 0;
  // count += 1;
}

/*
  ---------------------------------------------
  3.3 var
  ---------------------------------------------
  var는 오래된 JavaScript 코드에서 볼 수 있는 변수 선언 방식이다.
*/
{
  // var message = "안녕하세요.";
}

/*
  var는 함수 단위의 유효 범위를 가지며 같은 이름으로 다시 선언할 수 있다.
  이 때문에 예상하지 못한 오류가 생길 수 있다.

  새로 작성하는 코드에서는 다음 원칙을 따른다.
  1. 기본적으로 const를 사용한다.
  2. 재할당이 필요한 경우에만 let을 사용한다.
  3. var는 가능하면 사용하지 않는다. 기존 코드를 읽기 위한 목적으로 이해한다.
*/

/*
  ---------------------------------------------
  3.4 유효 범위
  ---------------------------------------------
  let과 const는 선언된 영역 안에서만 사용할 수 있다.
  자신이 선언된 block 밖에서는 호출 할 수 없다. (Block Scope)
*/

// if (true) {
//   const message = "블록 안의 값";
//   console.log(message);
// }

// console.log(message); // ReferenceError


/*
  ---------------------------------------------
  3.5 변수 이름 규칙
  ---------------------------------------------
  변수 이름에는 다음 문자를 사용할 수 있다.
  - 영문자와 유니코드 문자
  - 숫자
  - _
  - $
  숫자로 시작할 수는 없다.
*/

// const userName = "홍길동";
// const score20 = 90;
// const _temp = 10;


/* 다음과 같은 이름은 사용할 수 없다. (문법 오류가 발생하므로 주석 처리) */
// const 1score = 90;   // 숫자로 시작해서 오류
// const const = 10;    // 예약어를 사용해서 오류
// const my@score = 20; // _, $ 이외의 특수문자를 사용해서 오류

/* 관례적으로 변수의 이름은 카멜 표기법을 사용한다. */
{
  // const userName = "홍길동";
  // const totalPrice = 30000;
}
