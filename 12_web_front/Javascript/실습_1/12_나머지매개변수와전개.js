/*
  =============================================
  12. 나머지 매개변수(Rest Parameter)와 전개(Spread)
  =============================================
  나머지 매개변수와 전개 문법은 모두 마침표 세 개 ...를 사용한다.
*/

/*
  ---------------------------------------------
  12.1 나머지 매개변수(rest parameter)
  ---------------------------------------------
  함수에 전달된 여러 값을 배열로 모은다.
*/
{
  function sum(first, second, ...rest) {
    console.log(first);
    console.log(second);
    console.log(rest);
  }

  sum(10, 20, 30, 40, 50);
}
/*
  나머지 매개변수는 마지막 매개변수에 한 번만 사용할 수 있다.
*/

/*
  ---------------------------------------------
  12.2 배열 전개 (spread)
  ---------------------------------------------
  배열 전개(spread)는 배열의 요소들을 하나씩 꺼내어
  개별 값처럼 펼쳐 사용하는 문법이다.
*/
{
  // const numbers = [1, 2, 3];
  // const copied = [100, 200, ...numbers];

  // console.log(copied);
}

/* 배열을 합칠 때 사용할 수 있다. */
{
  // const a = [1, 2];
  // const b = [3, 4];

  // const result = [...a, ...b];

  // console.log(result); // [1, 2, 3, 4]
}

/*
  나머지 매개변수(rest parameter)를 받는 함수 호출에
  배열의 값을 펼쳐서 전달할 때 사용할 수 있다.
*/
{
  // const numbers = [10, 20, 30];

  // console.log(Math.max(...numbers)); // 30
}

/*
  ---------------------------------------------
  12.3 객체 전개
  ---------------------------------------------
  객체의 속성을 새 객체에 복사한다.
  이를 이용해 원본을 변경하지 않고 요소들을 복사/추가할 수 있다.
*/
{
  // const user = {
  //   name: "홍길동",
  //   age: 20,
  // };

  // const copiedUser = {
  //   ...user,
  //   address: "서울"
  // };

  // console.log(copiedUser); // { name: '홍길동', age: 20, address: '서울' }
}
