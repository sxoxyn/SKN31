// =============================================
// 14.2 기본 내보내기 - config.js
// =============================================
// 객체도 기본값으로 내보낼 수 있다.

const config = {
  apiUrl: "https://example.com",
  timeout: 3000,
};

export default config;
